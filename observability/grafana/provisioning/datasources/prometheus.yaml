apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: meshmart-prometheus
    url: http://prometheus:9090
    isDefault: true
    access: proxy
    editable: false
    jsonData:
      timeInterval: "10s"
